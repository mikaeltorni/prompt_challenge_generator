You will receive a problem statement meant for a prompt engineering challenge.

- Provide exactly 5 example pairs showing valid inputs with their expected outputs.
- All of the examples should be a bit creative. Avoid simple inputs such as single words or values; instead, use inputs written as full sentences.
- Do not allow obvious examples that are direct name/identifier lookups; examples should use more descriptive queries or natural phrasing that are generalizable to a broader set of entity-to-code translation prompts.
- At least one of the examples should result in `invalid_question` by prompt injection attack attempt
